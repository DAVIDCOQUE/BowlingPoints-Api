name: CI/CD Pipeline - BowlingPoints

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  # 🧱 CI: Compila, testea y analiza calidad en la rama dev
  build-and-analyze:
    name: Build, Test and Analyze (CI)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev' || github.event_name == 'pull_request'

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@8dfc4874178ecf8e9f1e8c6e5d73af4aab6ea67a # v4.2.1
        with:
          java-version: 17
          distribution: 'zulu'
          cache: maven

      - name: Cache SonarQube packages
        uses: actions/cache@6849a6489940f3a97298f54a0398b476faa495a9 # v4.1.2
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: 🧱 Build, test and generate JaCoCo report
        env:
          DB_URL: ${{ secrets.DB_URL }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: |
          echo "🏗️ Ejecutando build y tests..."
          mvn clean verify jacoco:report -B

      - name: 🔍 SonarQube analysis (with JaCoCo coverage)
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          echo "🚀 Iniciando análisis SonarQube..."
          mvn sonar:sonar \
            -Dsonar.projectKey=DAVIDCOQUE_BowlingPoints-Api \
            -Dsonar.host.url=$SONAR_HOST_URL \
            -Dsonar.login=$SONAR_TOKEN \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml \
            -Dsonar.java.binaries=target/classes

      - name: ✅ Check SonarQube Quality Gate
        uses: SonarSource/sonarqube-quality-gate-action@95b1cc6c02a15c8ecc00c07b1c2a75ed61a14055
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          scanMetadataReportFile: target/sonar/report-task.txt

      - name: 📦 Upload JAR artifact
        uses: actions/upload-artifact@1e5338d23a3f993bbf7d9d5bb379534f9f1525c7 # v4.4.3
        with:
          name: bowlingpoints-jar
          path: target/*.jar

  # 🚀 CD: Despliegue en la rama main
  docker-deploy:
    name: Build and Deploy Docker (CD)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: [build-and-analyze]

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Set up JDK 17
        uses: actions/setup-java@8dfc4874178ecf8e9f1e8c6e5d73af4aab6ea67a
        with:
          java-version: 17
          distribution: 'zulu'

      - name: Download JAR artifact
        uses: actions/download-artifact@50c7a5799a4cf7d4f645e74e1e1bc1a3c81264de # v4.1.7
        with:
          name: bowlingpoints-jar
          path: target/

      - name: Log in to Docker Hub
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASS }}

      - name: Build Docker image
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          echo "🐳 Construyendo imagen Docker..."
          docker build -t $DOCKERHUB_USERNAME/bowlingpoints:latest .

      - name: Push Docker image
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          echo "📦 Subiendo imagen a Docker Hub..."
          docker push $DOCKERHUB_USERNAME/bowlingpoints:latest

      - name: Deploy to AWS EC2
        uses: appleboy/ssh-action@3ca8a7c5359ac6ad91aa47f1946ece1c3b025004
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ec2-user
          key: ${{ secrets.AWS_SSH_KEY }}
          script: |
            echo "🚀 Desplegando en AWS EC2..."

            # 🧹 Detener y eliminar contenedores viejos
            docker stop bowlingpoints || true
            docker rm bowlingpoints || true
            docker stop bowlingpoints-app || true
            docker rm bowlingpoints-app || true

            # 🧹 Eliminar todas las imágenes del backend (sin tocar postgres)
            docker images | grep bowlingpoints | awk '{print $3}' | xargs docker rmi -f || true

            # 🔄 Descargar la nueva imagen
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/bowlingpoints:latest

            # 🚀 Iniciar el nuevo contenedor limpio
            docker run -d --name bowlingpoints -p ${{ secrets.APP_PORTS }} \
              -e DB_URL="${{ secrets.DB_URL }}" \
              -e DB_USER="${{ secrets.DB_USER }}" \
              -e DB_PASSWORD="${{ secrets.DB_PASSWORD }}" \
              --restart always \
              ${{ secrets.DOCKERHUB_USERNAME }}/bowlingpoints:latest
